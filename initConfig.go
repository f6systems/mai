//filename=initConfig.go
//goal=update code base to have a configuration process to run when initialized (starting).
package main

import (
	"log"
)

func init() {
	log.Printf("[initConfig:init()] - init().\n")
}
